import { LayoutDashboard, Users, CalendarCheck, CalendarMinus, Timer, Wallet, Settings, UsersRound, Network } from 'lucide-react';
export default function Sidebar() { return <aside className="w-[260px] bg-slate-900 text-slate-300 flex flex-col h-full flex-shrink-0 shadow-xl"><div className="h-[72px] flex items-center px-6 border-b border-slate-800"><div className="flex items-center gap-2 text-white font-bold text-xl"><div className="w-8 h-8 rounded bg-gradient-to-br from-blue-500 to-blue-700 flex items-center justify-center"><Network size={20} /></div>zentrix</div></div><nav className="flex-1 overflow-y-auto py-6 px-3 space-y-1"><p className="px-3 text-xs font-semibold text-slate-500 uppercase">Main Menu</p><Nav icon={<LayoutDashboard size={18} />} label="Dashboard" active /><Nav icon={<Users size={18} />} label="Directory" /><p className="px-3 pt-4 text-xs font-semibold text-slate-500 uppercase">Time & Operations</p><Nav icon={<CalendarCheck size={18} />} label="Attendance" /><Nav icon={<CalendarMinus size={18} />} label="Leave Requests" badge="8" /><Nav icon={<Timer size={18} />} label="Live Sessions" /><p className="px-3 pt-4 text-xs font-semibold text-slate-500 uppercase">Finance & Growth</p><Nav icon={<Wallet size={18} />} label="Payroll" /><Nav icon={<UsersRound size={18} />} label="Recruitment" /></nav><div className="p-4 border-t border-slate-800 flex items-center gap-3"><div className="w-9 h-9 rounded bg-slate-700 text-white flex items-center justify-center">A</div><div className="flex-1"><b className="text-sm text-white">Admin User</b><p className="text-xs text-slate-400 m-0">Workspace Owner</p></div><Settings size={18} /></div></aside> }
function Nav({ icon, label, active, badge }: { icon: React.ReactNode, label: string, active?: boolean, badge?: string }) { return <a href="#" className={`flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm ${active ? 'bg-blue-600/10 text-blue-400 border border-blue-600/20' : 'text-slate-400 hover:bg-slate-800 hover:text-white'}`}>{icon}<span>{label}</span>{badge && <span className="ml-auto bg-amber-500 text-white px-2 rounded-full text-[10px]">{badge}</span>}</a> }