import {useEffect} from 'react';export default function Toast({message,type,onClose}:{message:string,type:string,onClose:()=>void}){useEffect(()=>{const t=setTimeout(onClose,4000);return()=>clearTimeout(t)},[onClose]);return <div className="fixed bottom-6 right-6 bg-white border px-4 py-3 rounded-xl shadow-lg z-50"><b>{type==='success'?'Success':'Action Complete'}</b><p className="text-xs text-slate-500">{message}</p></div>}